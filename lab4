#include <stdio.h>
    
int main() {
    
long unsigned int charCount = 0;
    
    while ( getchar() != EOF ) {
    
        ++charCount;
    }
    
    printf("Number of chars are %lu ", charCount);
    return 0;
}
