/*
 * Filename: counting.c
 *
 * Description: counts the number of characters, words and lines in a text file
 *
 * Author: Anushka Kulasekara
 * Date: October 19, 2021
 */




#include <stdio.h>

int isWordchar( char aWord) {
    int result = 0;
    
    if( aWord != '\'' ){

    result = 1;
    }
    if (aWord >= 'A' && aWord <= 'Z'){

    result =1;
    }


    if (aWord >= 'a' && aWord <= 'z'){

    result =1;
    }


    return result;
}
    
int main() {
    
long unsigned int charCount = 0;
long unsigned int lineCount = 0;
long unsigned int wordCount = 0;
int aChar;
    
    while ( (aChar = getchar()) != EOF ) {
    
        charCount++;
        
        if ( isWordchar(aChar)){
            
            
            if(aChar == ' ' || aChar == '\n') {
                
                wordCount++;
            }

            if(aChar == '\n' && aChar == ' '){

                wordCount--;
            }
        }

        if ( aChar == '\n'){

            lineCount++;
        }
    }

    printf( "%lu %lu %lu\n", charCount, wordCount, lineCount );
    
    return 0;

}



////////////////////////////////////////////////////////////////////
///*
 * Filename: counting.c
 *
 * Description: counts the number of characters, words and lines in a text file
 *
 * Author: Anushka Kulasekara
 * Date: October 19, 2021
 */


#include <stdio.h>

int isWordchar( char aLetter) {
    int result = 0;
    
    if (( aLetter != '\'' ) && (aLetter >= 'A' && aLetter <= 'Z') || (aLetter >= 'a' && aLetter <= 'z')) {



    result =1;
    }


    return result;
}
    
int main() {
    
long unsigned int charCount = 0;
long unsigned int lineCount = 0;
long unsigned int wordCount = 0;
int aChar;
int aWord;
    
    while ( (aChar = getchar()) != EOF ) {
    
        charCount++;
    
            
            
            if(aChar == '\0' || aChar == '\n') {
                
                aWord = 1;
                
                if ( isWordchar(aChar)){
                    
                    if(aWord == 1){
                        
                        wordCount++;
                        aWord = 0;
                    }
                
            }


        }

        if ( aChar == '\n'){

            lineCount++;
        }
    }

    printf( "%lu %lu %lu\n", charCount, wordCount, lineCount );
    
    return 0;

}

